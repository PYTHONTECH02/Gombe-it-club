[build]
  base    = "artifacts/gombe-ict-club"
  command = "npm install && npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
